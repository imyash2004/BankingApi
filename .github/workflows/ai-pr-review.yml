name: PR Reviewer with OpenAI

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  pr-reviewer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: Set up Python environment
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openai requests

      - name: Get changed files
        id: files
        run: |
          # Get the base and head commit from GitHub context
          base_commit=${{ github.event.pull_request.base.sha }}
          head_commit=${{ github.sha }}

          echo "Base commit: $base_commit"
          echo "Head commit: $head_commit"

          # Get the list of changed files in the PR using GitHub context
          changed_files=$(git diff --name-only $base_commit $head_commit)

          if [ -z "$changed_files" ]; then
            echo "No files changed."
            echo "::set-output name=review_result::No changes detected."
          else
            # Convert changed files to array and pass to Python script
            changed_files_array=($changed_files)
          
            # Run Python script for review, passing the OpenAI API key from secrets
            python3 pr-review/review_pr.py "${changed_files_array[@]}"
          fi
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Comment on PR
        uses: peter-evans/create-or-update-comment@v2
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## Automated PR Review by OpenAI
            ${{ steps.files.outputs.review_result }}
